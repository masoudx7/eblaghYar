import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ir.eblaghyar.app',
  appName: 'ابلاغیار',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
